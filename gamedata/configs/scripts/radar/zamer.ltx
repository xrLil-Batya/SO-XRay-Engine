[logic]
active = sr_idle
 
[sr_idle]
on_actor_inside = {=actor_has_item(pchm_2m)} sr_timer@1 %=play_sound_looped(pchm_2m)%

[sr_timer@1]
type = dec
start_value = 30000
on_actor_outside = sr_idle %=stop_sound_looped(pchm_2m)%
on_value = 1|%=stop_sound_looped(pchm_2m)%| 0| nil 
;on_info = {+bar_deactivate_radar_done} nil
;%+sar_finish_decoding% 